#include <stdio.h>
/**
* main - Entry point
* Return: Always 0 (Success)
*/
int main(void)
{
char ch, sh;
for (ch = 'A'; ch <= 'Z'; ch++)
{
putchar(ch);
}
for (sh = 'A'; sh <= 'Z'; ch++)
{
putchar(sh);
}
return (0);
}
