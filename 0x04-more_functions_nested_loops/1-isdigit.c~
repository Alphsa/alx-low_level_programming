#include "main.h"
/**
*checks for a digit (0 through 9)
* of a number.
* @n: input number as an integer.
* Return: last digit.
*/
int _isdigit(int c)
{
int c;
l = n % 10;
if (l < 0)
{
_putchar(-l + 48);
return (-l);
}
else
{
_putchar(l + 48);
return (l);
}
}
